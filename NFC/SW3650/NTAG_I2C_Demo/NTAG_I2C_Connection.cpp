#include "NTAG_I2C_Connection.h"

using namespace NTAG_I2C_Demo;
/*				TO CONTROL DEPENDENCIES
NTAG_I2C_Connection::NTAG_I2C_Connection()
{
	rf_commands = new RF_Commands();
}

long NTAG_I2C_Connection::establishConnection(int mode, DWORD* pAP)
{
	rf_commands->establishConnection(mode, pAP);
}

void NTAG_I2C_Connection::releaseConnection(long lReturn)
{
	rf_commands->releaseConnection(lReturn);
}

*/